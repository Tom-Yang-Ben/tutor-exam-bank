# docs/kc-review-數學.md — 數學科知識點種子檔抽查紀錄

> 版本 v2.1 | 2026-09-25（章節重整：34 章 → 52 章，見第 9 節；重整後的審查修正見第 9.7 節；v1.1 的審查修正見第 8 節） | 分支 `stage5/ch-c` | 對應：`docs/chapter-restructure.md` 第 3.3 條，`docs/interfaces-stage5.md` 第 3.4、3.5、4.6 條
> 受檢檔案：`exam_pro/config/kc/數學.json`、`exam_pro/config/kc/code-maps/code-map-數學-2026-09.json`。本檔列出 AI 草擬時最沒把握之處，供 Owner 審定時優先檢查；**不是**審定結果。
> 〔知識點審定單 2026-09-26〕v2.2 | 2026-09-26 | 分支 `dec/r2-math-kc`：落實 Owner 在知識點審定單第二輪（M1–M21）與第一輪 A9 備註的決定。下文各項的原文保留，Owner 的決定以〔知識點審定單 2026-09-26 代號〕加註在該項；總表、做法與驗證見**第 10 節**。新的代碼對照檔 `exam_pro/config/kc/code-maps/code-map-數學-2026-09-26.json`。
> 〔知識點審定單 2026-09-26 補答〕v2.3 | 2026-09-26 | 分支 `dec/r2-math-kc2`（接在 `dec/r2-math-kc` 之後）：落實 Owner 後來補答的 M15（複數的幾何意涵拆成 3 條）、M17（兩個都刪），以及勘根定理改寫成連續函數版本。見**第 10.5 節**；對照檔仍是同一份，補上本次的刪除。
> 〔重練與收尾決策單 2026-09-26〕v2.4 | 2026-09-26 | 分支 `dec/r2-math-kc3`（接在 `dec/r2-math-kc2` 之後）：登錄 Owner 在「重練與收尾決策單」對 K8（邏輯 2 條的位置）、K9（M10 口語版一起改）、K10（`複數的幾何意涵.03` 的和角公式先備）的答覆；K8、K9 維持現狀，K10 拿掉一個先備。見**第 10.6 節**；對照檔沒有改（代碼沒有變動）。

## 0. 總覽

| 項目 | 數值 |
|---|---|
| 知識點總數 | 254（52 章全數涵蓋，章名逐字取自 `config/chapterPlan.js` 的 `PLAN_VOLUMES['數學']`） |
| 第一冊 | 46（實數 7、絕對值 6、指數與對數 4、直線方程式 5、圓方程式 6、多項式的運算與應用 8、多項式函數的圖形 6、多項式不等式 4） |
| 第二冊 | 58（數列與遞迴關係 5、級數 4、集合與計數原理 5、排列 5、組合 4、二項式定理 3、古典機率 3、期望值 3、一維數據分析 6、二維數據分析 5、直角三角形的邊角關係 3、廣義角與極坐標 4、正弦與餘弦定理 4、三角測量 4） |
| 第三冊(A/B) | 35（三角函數的圖形 3、和角與差角公式 3、三角函數的疊合 3、指數函數與對數函數 7、向量的加減與係數積 6、向量內積 6、面積與行列式 7） |
| 第四冊(A/B) | 44（空間概念與座標系 5、空間向量內積 5、外積 6、平面方程式 6、空間直線方程式 6、條件機率與貝氏定理 3、一次方程組 4、矩陣的運算 5、矩陣的應用 4） |
| 選修數學 | 71（數列的極限 6、函數的極限 6、微分導函數 7、函數圖形與極值 7、定積分與面積 7、隨機變數 3、二項分布與幾何分布 5、常態分配 5、複數與多項式方程式 5、複數的幾何意涵 5、拋物線 4、橢圓 4、雙曲線 4、線性規劃 3） |
| 來源 | 舊檔 211 條全部保留（對照檔沒有 `null`）：148 條 code 不變、63 條改 code；**新寫 43 條**。新舊檔之間沒有「同一個 code 代表不同知識點」的情形（見第 2.2 節第 1 點） |
| `status: approved` | 4（只有第 3.5 條 Owner 核可的 4 條，code、內容、先備逐字不動，見第 5 節） |
| `status: draft` | 250 |
| 先備關係 | 320 條，全部在數學科內；每個知識點 0–3 個；無環 |
| 跨科先備 | 0 條（見第 4 節） |
| `curriculum_code` | 全部 `null`（見第 6 節） |
| 口語版長度 | 62–231 字，平均 144 字；全部 2–4 句（以「。！？」切句統計） |

〔知識點審定單 2026-09-26〕上表是 v2.1 的數字。第二輪落實後：知識點 **255**（刪 1、新增 2；另搬 2 條）；第一冊 45（多項式函數的圖形 6→5）、第二冊 60（集合與計數原理 5→7）、第三冊 35（不變）、第四冊 44（空間概念與座標系 5→6、空間向量內積 5→4）、選修 71（函數的極限 6→7、二項分布與幾何分布 5→4）；`approved` 仍是 4 條、一字未動；先備 322 條、無環，全檔已沒有指向後面章節的先備。細節見第 10 節。〔知識點審定單 2026-09-26 補答〕Owner 補答 M15 後：知識點 **253**（複數的幾何意涵 5→3：刪 3、新增 1）；選修 69；`approved` 仍是 4 條；先備 321 條、無環。細節見第 10.5 節。〔重練與收尾決策單 2026-09-26 K10〕`複數的幾何意涵.03` 拿掉和角公式這個先備後：知識點仍是 **253**；先備 **320** 條、無環。細節見第 10.6 節。

驗證（2026-09-25 v2.1 修正後實跑，細節見第 9.6 節）：

- 契約指定的指令（`validateSeeds`，章節表用 `PLAN_CHAPTERS`＋化學）：
  - 數學單檔、數學＋化學：0 error、0 warning。數學 254 個、52 章、已審定 4 個。
  - 數學（本分支）＋ CH-D worktree 已 commit 的 `物理.json`、`化學.json`（唯讀取用，不複製進本分支）：**0 error、0 warning**。
  - 數學＋本分支的舊 `物理.json`＋化學：17 個 error，全部屬於物理（CH-D 的範圍，整合後消失）：15 個舊物理章節表，2 個指向舊代碼 `MATH.三角函數的定義.01`。

## 1. 最沒把握的條目

### 1.1 v1 的 10 條（已換成新代碼）

排序依「錯了對學生的傷害」由大到小。這些條目本次只換章或換代碼，內容沒有改。

| # | code（舊 code） | 原因 |
|---|---|---|
| 1 | `MATH.一維數據分析.03` 百分位數與盒狀圖 | 說明寫入「n·k/100 為整數 m 時取第 m 與 m+1 筆的平均，否則無條件進位取第 ⌈n·k/100⌉ 筆」。這是我記得的台灣教科書定義，但沒能對照 Ben 使用的版本；四分位數在不同教材（含計算機、Excel）有多種慣例，算出來的 Q₁、Q₃ 會不同。口語版也照這個取法講了一次，版本若不同，說明與口語版要一起改。 **〔知識點審定單 2026-09-26 M6〕Owner 選 1：維持 n×k% 法（現況），不改。** |
| 2 | `MATH.矩陣的應用.02` 轉移矩陣（`矩陣的加減與乘法.06`） | 採「每一**行**和為 1、狀態寫成行向量、Xₙ₊₁=AXₙ」的寫法。部分教材與國際慣例是「每一**列**和為 1、列向量右乘」。若 Ben 用的版本相反，說明與口語版的「行／列」必須對調，否則學生會照錯的方向乘。 **〔知識點審定單 2026-09-26 M7〕Owner 選 1：維持每一行和為 1、Xₙ₊₁＝AXₙ（現況），不改。** |
| 3 | `MATH.一維數據分析.04` 變異數與標準差 | 以母體標準差（除以 n）為主，括號註明樣本標準差除以 n−1。108 高一教材是否介紹 n−1、學測題以哪一個為準，依版本而定。 **〔知識點審定單 2026-09-26 M8〕Owner 選 2：只寫除以 n，不提 n−1。已刪說明括號裡的「樣本標準差以 n−1 為分母」，「母體變異數」改稱「變異數」；口語版原本就只講除以 n、沒有提 n−1，未改。** |
| 4 | `MATH.常態分配.05` 抽樣與信賴區間 | 不確定 108 課綱把信賴區間放在選修數學甲還是乙。95% 信賴區間寫成「約 ±2 倍標準誤（精確值 1.96）」，課本用 2 還是 1.96 依版本。 **〔知識點審定單 2026-09-26 M9〕Owner 選 1：維持 2 倍、括號提 1.96（現況），不改。** |
| 5 | `MATH.二項分布與幾何分布.05` 幾何分布（`隨機變數.06`） | v1 不確定幾何分布是否在範圍內；新章表有「二項分布與幾何分布」，這一點已由章名確認。仍請確認說明中的 E(X)=1/p 是否為課本要求的內容。 **〔知識點審定單 2026-09-26 M10〕Owner 選 4（兩個都沒教）：說明刪掉「E(X)=1/p」；口語版原本也講「期望值是 p 分之 1，平均要擲 6 次」，一併刪掉，改用「第 3 次才出現 6 的機率」當例子（口語版這一處是依同一決定延伸，Owner 若要保留請告知）。sort 5→4。** **〔重練與收尾決策單 2026-09-26 K9〕Owner 選 1：同意口語版一起改，維持現稿（見第 10.6 節）。** |
| 6 | `MATH.微分導函數.05` 乘積與合成函數的微分 | 108 選修數學甲的微分範圍是否包含一般的連鎖律，或只到 (ax+b)ⁿ 這個特例，不確定。說明兩者都寫了。 **〔知識點審定單 2026-09-26 M11〕Owner 選 1：兩種都寫（現況），不改。** |
| 7 | `MATH.多項式的運算與應用.08` 插值多項式（`多項式除法.08`） | 印象中 108 第一冊有「插值多項式」，但拉格朗日形式與牛頓形式要教到哪一種、是否屬於考試範圍，依版本而定。 **〔知識點審定單 2026-09-26 M12〕Owner 選 1：兩種都寫、口語版用牛頓形式（現況），不改。** |
| 8 | `MATH.複數與多項式方程式.03` 代數基本定理與虛根成對（`三次函數.06`） | 已搬到正確的章（v1 只能放在三次函數）。複數的四則運算已拆成新的 `.01`，本條的說明與口語版因此改寫（見第 9.3 節）。代數基本定理在 108 是敘述性帶過還是要求使用，仍不確定。 |
| 9 | `MATH.線性規劃.01` 二元一次不等式的圖形（`直線方程式.06`） | v1 不確定 108 第一冊是否保留這個主題；新章表把它放進選修的「線性規劃」，已依此搬移。 |
| 10 | `MATH.實數.07` 算幾不等式 | 內容沒有疑慮，但 108 教材把它放在第一冊「實數」還是其他章節（例如與柯西不等式並列），不確定。 **〔知識點審定單 2026-09-26 M13〕Owner 選 1：留在第一冊「實數」（現況），不改。** |

其他次要疑點（內容正確，屬於寫法或範圍的選擇）：

- `MATH.期望值.03` 期望值的加法性質：高一「期望值」是否教「和的期望值等於期望值的和」不確定；它與 `MATH.隨機變數.03` 有部分重疊。**〔知識點審定單 2026-09-26 M2〕Owner 選 1：(甲) 乘 a 加 b、(乙) 和的期望值都有教（現況），不改。**
- `MATH.面積與行列式.07` 二元一次聯立解的判別（原 `克拉瑪公式.02`）：「Δ=Δx=Δy=0 ⇒ 無限多解」只在兩式都代表直線時成立（說明已加上前提）。有些課本直接寫「無限多解」，有些寫「無限多解或無解」。**〔知識點審定單 2026-09-26 M14〕Owner 選 1：維持「兩式都表示直線時無限多解」的寫法（現況），不改。**
- `MATH.一維數據分析.01` 資料的整理與圖表：「右偏時平均數通常大於中位數」寫了「通常」，因為這不是定理。
- v1 列的「極坐標只能放在三角函數的定義」已解決：現在是 `MATH.廣義角與極坐標.04`，與 108 第二冊的單元名稱一致。

### 1.2 本次新寫與改寫的條目（含全部新章）

新寫的 43 條逐條自我複查過數學內容（公式、條件、反例、例題數字都重算過），口語版照第 3.5 條寫（對學生說、講用法與常錯、不用比喻、無 LaTeX、2–4 句、一件事）。不確定的是**108 範圍**與**版本慣例**，依傷害大小排列：

| # | code | 原因 |
|---|---|---|
| 1 | `MATH.指數與對數.04`、`.05`，`MATH.指數函數與對數函數.03`、`.04` | 前提：108 第一冊只教指數律與**常用對數**，一般底數的對數與換底公式在第三冊。v2.1 照這個前提把兩冊的分界做完整（v2.0 只拆了換底，第一冊的定義與對數律仍是一般底數，第三冊反而沒有一般底數的定義可標）：第一冊 `指數與對數.04` 常用對數的定義、`.05` 常用對數的運算性質都只談以 10 為底；第三冊新寫 `指數函數與對數函數.03` 一般底數的對數與對數律，`.04` 換底公式以它為先備。CH-B 的 eval 改標（`stage5/ch-b`，本次審查時尚未 commit）也是這個分界：log₂、log₃ 的題標到「指數函數與對數函數」，只有以 10 為底的位數估計題留在「指數與對數」。**若 Ben 的第一冊就教一般底數**，改走另一個方向：`指數與對數.04`、`.05` 改回一般底數，換底公式搬回「指數與對數」（新 code `.09`：`.02`、`.03`、`.07`、`.08` 是空出的舊序號，不重用），第三冊的 `.03` 刪除，並請 CH-B 把那些 log₂、log₃ 題改標回第一冊。 |
| 2 | `MATH.複數的幾何意涵.01`–`.05` | 整章新寫：複數平面與絕對值、極式、極式乘除與旋轉、棣美弗定理、n 次方根。數學內容有把握；不確定選修數學甲是否教到「n 次方根與正 n 邊形」「1 的 n 次方根之和為 0」，以及棣美弗定理是否要求負整數次方。 **〔知識點審定單 2026-09-26 M15〕暫不處理：Owner 沒有選方案，備註「複數平面、乘除有教，其他都沒教」。這個備註回答的範圍比題目大：題目只問說明裡的 (甲)(乙)(丙) 三句要不要刪，備註卻像是在說整章 5 條只教 `.01` 複數平面與複數的絕對值、`.03` 極式的乘除與旋轉。照字面刪到只剩 2 條會少於每章 3 條的下限，而且 `.03` 以 `.02` 複數的極式為先備，極式要不要留也不清楚，需要再問 Owner；本分支這一章一字未改。** **〔知識點審定單 2026-09-26 補答 M15〕Owner 補答：課本只教複數平面與乘除，極式、棣美弗定理、n 次方根都沒教；選「拆成 3 條、保留這章」。現為 `.01` 複數平面與絕對值、`.06` 複數加減的幾何意義（新）、`.03` 複數乘除的幾何意義（改寫），`.02`、`.04`、`.05` 刪除（第 10.5 節）。** |
| 3 | `MATH.拋物線.01`–`.04`、`MATH.橢圓.01`–`.04`、`MATH.雙曲線.01`–`.04` | 三章新寫。(a) 符號慣例：拋物線用 y²=4cx（c 為焦點到頂點的距離），部分教材寫 y²=4px；橢圓與雙曲線的 a、b、c 照常見寫法。(b) 範圍：`橢圓.04` 的焦點三角形面積公式 b²·tan(θ/2) 放在說明裡當例子，是否超出課本不確定；正焦弦只寫了拋物線的。(c) 退化情形（一點、無圖形、兩相交直線）寫在 `橢圓.03`、`雙曲線.04`，課本是否要求不確定。(d) 沒有寫圓錐曲線的切線與參數式，因為不確定 108 是否包含。 **〔知識點審定單 2026-09-26 M16〕(a) Owner 選 1：拋物線用 c（現況），不改。〔知識點審定單 2026-09-26 M17〕(b)(c) 橢圓焦點三角形面積公式與退化情形：Owner 未作答，暫不處理、維持現稿。** **〔知識點審定單 2026-09-26 補答 M17〕Owner 補答選「兩個都刪」：`橢圓.04` 刪 b²·tan(θ/2)，保留用 2a 與餘弦定理算的方法；`橢圓.03`、`雙曲線.04` 刪配方後的退化情形（說明與口語版）；代碼不變（第 10.5 節）。** |
| 4 | `MATH.二項分布與幾何分布.04` 二項分布中機率最大的次數 | 推導與結論（不超過 (n+1)p 的最大整數；(n+1)p 為整數時兩個）已驗算。不確定是否在 108 範圍內；若不在，刪除後本章仍有 4 條。 **〔知識點審定單 2026-09-26 M10〕Owner 選 4：沒教，已刪除。對照檔記為 `null`；刪除前確認沒有任何先備指向它。本章剩 4 條。** |
| 5 | `MATH.和角與差角公式.03` 兩直線的夾角 | tanθ=\|(m₁−m₂)/(1+m₁m₂)\| 由差角公式推得，內容有把握（v2.1 補上 m₁≠m₂ 的條件）。不確定龍騰把它放在「和角公式」的應用，還是第一冊直線方程式。 **〔知識點審定單 2026-09-26 M18〕Owner 選 1：留在第三冊「和角與差角公式」（現況），不改。** |
| 6 | `MATH.三角函數的圖形.03` 簡單三角方程式、`MATH.三角函數的疊合.02`、`.03` | 疊合一章原本只有 1 條，新寫的兩條是常見題型（二次齊次式降次後疊合、a sinx+b cosx=c 的解）。不確定 108 是否要求解三角方程式到一般解；說明只寫到「指定範圍內的所有解」。 **〔知識點審定單 2026-09-26 M19〕Owner 選 2：補上一般解。`三角函數的圖形.03` 的說明加「未限定範圍時寫一般解，如 sinx=1/2 的解為 x=30°+360°k 或 150°+360°k（k 為整數）」；`三角函數的疊合.03` 的說明加 sinx+√3cosx=1 → 2sin(x+60°)=1 → x=−30°+360°k 或 90°+360°k 的例子（已代回驗算）。兩條口語版照 Owner 核可的風格（對學生說「你」、短句、最後點出常錯）改寫並加上一般解，仍是 4 句。** |
| 7 | `MATH.多項式不等式.01`、`.02`、`.04` | 一元二次不等式、恆正恆負的條件、由解集反求係數。內容有把握；`.04` 是常見考法但不是課綱條文。 |
| 8 | `MATH.集合與計數原理.01`、`.02`、`.04` | 集合的表示與子集、集合運算與笛摩根定律、樹狀圖。不確定 108 第二冊是否含笛摩根定律與差集；**邏輯（命題、充分與必要條件）仍沒有知識點**（見第 2 節）。 **〔知識點審定單 2026-09-26 M20〕Owner 選 1：差集、笛摩根定律都有教（現況），不改。〔知識點審定單 2026-09-26 M4〕邏輯已補 2 條：`集合與計數原理.06` 命題與且、或、非，`.07` 充分條件與必要條件（第 10 節）。** |
| 9 | `MATH.線性規劃.02`、`.03` | 頂點法、平行線法、應用題與整數解。整數解「比較鄰近格子點」是教學上的做法，不是一般保證最佳的演算法；口語版照課本常見說法寫。 |
| 10 | 其餘新寫：`多項式函數的圖形.01`、`.05`，`數列與遞迴關係.04`，`二項式定理.03`，`直角三角形的邊角關係.03`，`指數函數與對數函數.07`，`矩陣的運算.04`，`二項分布與幾何分布.01`，`複數與多項式方程式.01`、`.02`、`.05` | 屬於基本內容，數學上有把握。`數列與遞迴關係.04` 只寫到累加法，沒有寫 aₙ₊₁=p·aₙ+q 這一型（不確定 108 是否要求）；`矩陣的運算.04` 方陣的乘冪沒有寫凱萊–漢米爾頓定理。 |

改寫的舊條目（內容也請一併看）：`MATH.直角三角形的邊角關係.02` 的口語版最後一句（原本講「依象限判斷正負」，搬到只談銳角的章後改成 sin²θ 與 sin(θ²) 的區別）、`MATH.複數與多項式方程式.03`（見上表第 8 條）、`MATH.指數與對數.04`、`.05`（改成只談常用對數，見第 1.2 節第 1 條）。改動細節見第 9.3 節。

## 2. 章節切法需要 Owner 決定之處

### 2.1 v1 的 11 點處理結果

| # | v1 的問題 | 狀態 | 處理 |
|---|---|---|---|
| 1 | 「三次函數」裝了非三次函數的內容 | **已依重整處理** | 二次函數 → `多項式函數的圖形.02`；多項式不等式 → `多項式不等式.03`；複數與代數基本定理 → `複數與多項式方程式.03`；根與係數 → `複數與多項式方程式.04`；勘根定理 → `多項式函數的圖形.06`。 **〔知識點審定單 2026-09-26 M3〕勘根定理再搬到選修 `函數的極限.07`（見第 2.2 節第 6 點）。** |
| 2 | 「正弦與餘弦定理」含和差角、倍角半角、疊合 | **已依重整處理** | → `和角與差角公式.01`、`.02`，`三角函數的疊合.01`。 |
| 3 | 「古典機率」含條件機率、獨立事件、貝氏定理 | **已依重整處理** | → `條件機率與貝氏定理.01`–`.03`。 |
| 4 | 「排列」含排容原理；集合與邏輯沒有知識點 | **部分處理，邏輯仍待 Owner 決定** | 加法乘法原理、排容原理 → `集合與計數原理.03`、`.05`；新寫集合 2 條、樹狀圖 1 條。「邏輯」（命題、且或非、充分必要條件）新章表沒有對應章，仍沒有知識點。 **〔知識點審定單 2026-09-26 M4〕Owner 選 1：放進第二冊「集合與計數原理」，新增 2 條：`.06` 命題與且、或、非（且或非對應交集、聯集、補集），`.07` 充分條件與必要條件（p⇒q ⇔ P⊆Q）。查題別名加「邏輯」「命題」「充分條件」「必要條件」「充分必要條件」。已處理。** **〔重練與收尾決策單 2026-09-26 K8〕Owner 選 1：兩條維持在集合運算之後、計數原理之前（sort 3、4），不改（見第 10.6 節）。** |
| 5 | 「組合」含二項式定理，「數列與級數」含數學歸納法 | **已依重整處理（歸納法的冊別待確認）** | 二項式定理 → `二項式定理.01`、`.02`；數學歸納法 → `數列與遞迴關係.05`。數學歸納法在 108 是否仍在第二冊，我沒有把握。 **〔知識點審定單 2026-09-26 A9〕Owner 備註「數學歸納法是數列與級數」：與草案（第二冊 `數列與遞迴關係.05`）一致，不改。** |
| 6 | 二元克拉瑪放在克拉瑪章；三階行列式放在外積；高斯消去放在克拉瑪章 | **已依重整處理** | 二元克拉瑪 → `面積與行列式.06`、`.07`（照 108 第三冊）；三階行列式仍是 `外積.04`；高斯消去、三元克拉瑪、三平面 → `一次方程組.01`–`.04`。 |
| 7 | 「空間向量內積」.01 是空間向量的坐標運算 | **未變動，仍待 Owner 決定** | 新章表仍沒有「空間向量」章。 **〔知識點審定單 2026-09-26 M5〕Owner 選 2：搬到「空間概念與座標系」，代碼 `空間向量內積.01` → `空間概念與座標系.06`（sort 6），內容不動；指向它的 3 條先備（`空間向量內積.02`、`外積.01`、`空間直線方程式.01`）全部改指新代碼；「空間向量內積」剩 4 條，sort 改為 1–4（code 沿用 .02–.05）。已處理。** |
| 8 | 「期望值」（第二冊）與「隨機變數」（選修）重疊 | **未變動，仍待 Owner 決定** | 兩章都還在，分工照 v1。 **〔知識點審定單 2026-09-26 M1、A9〕Owner 備註「期望值放在古典機率、隨機變數是數甲機率統計」；M1 選 1：意思是期望值屬高一機率、跟著古典機率教，「古典機率」「期望值」兩章維持分開（現況）。M2 選 1：期望值.03 的加法性質維持。分工照草案，不改。** |
| 9 | 「指數與對數」到 8 條上限，應用題沒有獨立成條 | **已依重整處理** | 拆成第一冊 `指數與對數`（4 條，只談常用對數）與第三冊 `指數函數與對數函數`（7 條），新寫「一般底數的對數與對數律」「換底公式」「指數與對數的應用」。拆法的前提見第 1.2 節第 1 條。 |
| 10 | 線性變換放在矩陣章，推移併在旋轉鏡射 | **已依重整處理（推移仍待確認）** | → `矩陣的應用.03`、`.04`。108 是否要求推移矩陣仍待確認。 **〔知識點審定單 2026-09-26 A9〕Owner 備註「推移矩陣是矩陣」：與草案（`矩陣的應用.04` 含推移）一致，不改。** |
| 11 | `平面方程式.06` 的先備指向後一章 `空間直線方程式.01` | **未變動，仍待 Owner 決定** | 兩章仍同冊、先後不變。這是全檔唯一指向後面章節的先備。 **〔知識點審定單 2026-09-26 M21〕Owner 選 3：留在「平面方程式」、代碼不變，先備 `空間直線方程式.01` 改為 `向量的加減與係數積.03`（係數積與向量平行：設垂足＝P＋t 倍法向量只用到係數積）。已處理；全檔已沒有指向後面章節的先備。** |

**仍沒有涵蓋的課綱內容**：邏輯、球面方程式、三角函數與指數對數函數的微積分、旋轉體體積、抽樣方法、圓錐曲線的切線。v1 列的線性規劃、集合、一次函數已補上。**〔知識點審定單 2026-09-26 M4〕邏輯已補上（`集合與計數原理.06`、`.07`）；其餘照舊。**

### 2.2 本次重整新產生、需要 Owner 決定之處

1. **同名章留下的舊條目沿用舊 code，空出的序號不再使用**（v2.1 起，與 CH-D 的物理做法一致）。v2.0 照「依新序號重編」讓後面的條目往前遞補，結果有 8 個 code 在新舊檔代表不同知識點（例如 `MATH.排列.01` 舊為加法乘法原理、新為直線排列）；`kc:load` 以 code upsert，若 DB 載入過舊檔，這 8 列會被原地改成另一個知識點，題目標註跟著指錯而不報錯。現在的做法：

   | 章 | code（sort） | 空出的序號 |
   |---|---|---|
   | 指數與對數 | `.01` 指數律與有理指數（1）、`.04` 常用對數的定義（2）、`.05` 常用對數的運算性質（3）、`.06` 常用對數與位數估計（4） | `.02`、`.03`（搬到指數函數與對數函數）、`.07`、`.08`（同） |
   | 排列 | `.03` 直線排列（1）、`.04` 相鄰與不相鄰（2）、`.05` 不盡相異物排列（3）、`.06` 環狀排列（4）、`.07` 重複排列（5） | `.01`、`.02`（搬到集合與計數原理） |

   這兩章以外，code 的序號都等於 `sort`。新舊檔之間已沒有同 code 異義的情形（驗證見第 9.6 節）。仍要注意：`指數與對數.04`、`.05` 的內容從一般底數收窄成常用對數（第 1.2 節第 1 條），是同一個概念的範圍調整，不是換成另一個知識點。
2. **舊 code 消失**：`MATH.三次函數.*`、`MATH.數列與級數.*`、`MATH.三角函數的定義.*`、`MATH.多項式除法.*`、`MATH.矩陣的加減與乘法.*`、`MATH.克拉瑪公式.*`，以及 `指數與對數.02`、`.03`、`.07`、`.08`、`排列.01`、`.02`、`直線方程式.06`、`組合.05`、`.06`、`古典機率.04`–`.06`、`隨機變數.04`–`.06`、`正弦與餘弦定理.05`–`.07`（共 63 個）。若 DB 已載入舊檔，這些列在 `kc:load` 後會變成「DB 有、種子檔沒有」的孤兒（S5-22：不刪、只回報），題目上指向它們的標註要依對照檔改寫。第一次 `kc:load` 前，Owner 可用 `SELECT count(*) FROM knowledge_components WHERE subject='數學'` 確認是否為 0；是 0 就沒有影響。
3. **`一次方程組.01` 高斯消去法的先備改成空的**：舊的先備是「矩陣的意義與加減」，但新章表的「一次方程組」排在「矩陣的運算」之前（108 在線性方程組先介紹增廣矩陣，再談矩陣運算），留著會變成指向後面章節的先備。
4. **「以反方陣解聯立方程式」放在「矩陣的應用」**（`矩陣的應用.01`）。放「矩陣的運算」也說得通；我放在應用，讓兩章是 5 條與 4 條。**〔知識點審定單 2026-09-26 A9〕Owner 備註「反方陣也是矩陣」：兩章都屬矩陣單元，與草案一致，不改。**
5. **複數的冊別**：新章表把「複數與多項式方程式」放在選修。若 Ben 的第一冊在「多項式方程式」就教了虛數 i 與二次方程式的虛根，第一冊這類題會被分到選修的章。這是章節表的問題，不是知識點的問題，列在這裡供 Owner 參考。**〔知識點審定單 2026-09-26 A9〕Owner 備註「複數是數甲」：與章節表（選修）一致，不改。**
6. **勘根定理放在「多項式函數的圖形」**（`多項式函數的圖形.06`，照 `MIGRATION` 的預設去處）。108 可能在「多項式方程式」教；目前沒有對應的第一冊章。**〔知識點審定單 2026-09-26 M3、A9〕Owner 備註「勘根定理是數甲微積分」；M3 選 1：搬到選修「函數的極限」，獨立一條。`多項式函數的圖形.06` → `函數的極限.07`（sort 7，排在 `.06` 中間值定理之後），先備改為 `函數的極限.06`；`函數的極限.06` 原本以勘根定理為先備，已拿掉（否則成環）；查題別名「勘根定理」改指「函數的極限」；第一冊「多項式函數的圖形」剩 5 條。名稱、說明、口語版照搬未改（見第 10.3 節第 1 點）。** **〔知識點審定單 2026-09-26 補答〕Owner 補充：勘根定理的說明與口語版改寫成連續函數版本（f 在 [a,b] 連續且 f(a)f(b)<0），以中間值定理為先備；中間值定理只留一般敘述，加一句「勘根定理是它的特例」（第 10.5 節）。**

## 3. 各章基礎知識點（給其他科引用時核對）

契約要求其他科只引用「該章第 01、02 個」這類基礎知識點。下表列出各章 sort 1、2 的條目（前面的數字是 code 的序號），供整合階段核對跨科先備是否合理。指數與對數、排列兩章沿用舊 code，前兩條不是 01／02（見第 2.2 節第 1 點）。

| 章 | 條數 | sort 1／2 |
|---|---|---|
| 實數 | 7 | 01 實數的分類與數線；02 循環小數化為分數 |
| 絕對值 | 6 | 01 絕對值的意義；02 數線上的距離與分點 |
| 指數與對數 | 4 | 01 指數律與有理指數；04 常用對數的定義 |
| 直線方程式 | 5 | 01 斜率；02 直線方程式的各種形式 |
| 圓方程式 | 6 | 01 圓的標準式；02 圓的一般式與配方 |
| 多項式的運算與應用 | 8 | 01 多項式的除法原理；02 綜合除法 |
| 多項式函數的圖形 | 6 | 01 一次函數與常數函數的圖形；02 二次函數的圖形與最值 |
| 多項式不等式 | 4 | 01 一元二次不等式；02 二次式恆正或恆負的條件 |
| 數列與遞迴關係 | 5 | 01 數列的一般項與遞迴式；02 等差數列 |
| 級數 | 4 | 01 等差級數；02 等比級數 |
| 集合與計數原理 | 5 | 01 集合的表示法與子集；02 集合的運算與笛摩根定律 |
| 排列 | 5 | 03 直線排列；04 相鄰與不相鄰 |
| 組合 | 4 | 01 組合的意義與公式；02 巴斯卡公式 |
| 二項式定理 | 3 | 01 二項展開式的一般項；02 二項式係數的性質 |
| 古典機率 | 3 | 01 樣本空間與事件；02 古典機率的定義 |
| 期望值 | 3 | 01 期望值的定義與計算；02 公平遊戲與方案比較 |
| 一維數據分析 | 6 | 01 資料的整理與圖表；02 平均數、中位數與眾數 |
| 二維數據分析 | 5 | 01 散布圖與相關；02 相關係數 |
| 直角三角形的邊角關係 | 3 | 01 直角三角形的三角比；02 三角函數的基本關係 |
| 廣義角與極坐標 | 4 | 01 廣義角與同界角；02 廣義角的三角函數 |
| 正弦與餘弦定理 | 4 | 01 三角形面積公式；02 正弦定理 |
| 三角測量 | 4 | 01 仰角與俯角；02 方位角 |
| 三角函數的圖形 | 3 | 01 弧度量與扇形；02 三角函數的圖形與週期 |
| 和角與差角公式 | 3 | 01 三角函數的和角與差角公式；02 倍角與半角公式 |
| 三角函數的疊合 | 3 | 01 疊合公式與最大最小值；02 二次齊次式的降次與疊合 |
| 指數函數與對數函數 | 7 | 01 指數函數的圖形與性質；02 指數方程式與不等式 |
| 向量的加減與係數積 | 6 | 01 向量的意義與坐標表示；02 向量的加法與減法 |
| 向量內積 | 6 | 01 內積的意義；02 內積的坐標算法 |
| 面積與行列式 | 7 | 01 二階行列式的定義；02 二階行列式的性質 |
| 空間概念與座標系 | 5 | 01 空間中直線與平面的關係；02 三垂線定理 |
| 空間向量內積 | 5 | 01 空間向量的坐標運算；02 空間向量的內積與夾角 |
| 外積 | 6 | 01 外積的定義與坐標計算；02 外積的幾何意義 |
| 平面方程式 | 6 | 01 平面的法向量與點法式；02 平面方程式的一般式與截距式 |
| 空間直線方程式 | 6 | 01 空間直線的參數式；02 比例式與兩平面的交線 |
| 條件機率與貝氏定理 | 3 | 01 條件機率；02 獨立事件 |
| 一次方程組 | 4 | 01 高斯消去法；02 三元一次聯立的克拉瑪公式 |
| 矩陣的運算 | 5 | 01 矩陣的意義與加減；02 矩陣乘法 |
| 矩陣的應用 | 4 | 01 以反方陣解聯立方程式；02 轉移矩陣 |
| 數列的極限 | 6 | 01 數列的收斂與發散；02 數列極限的運算性質 |
| 函數的極限 | 6 | 01 函數極限與左右極限；02 函數極限的運算 |
| 微分導函數 | 7 | 01 平均變化率與導數的定義；02 導數定義式的變形 |
| 函數圖形與極值 | 7 | 01 函數的遞增與遞減；02 局部極值與一階檢定 |
| 定積分與面積 | 7 | 01 黎曼和與定積分的意義；02 定積分的基本性質 |
| 隨機變數 | 3 | 01 離散隨機變數與機率分布；02 期望值與變異數 |
| 二項分布與幾何分布 | 5 | 01 伯努利試驗；02 二項分布 |
| 常態分配 | 5 | 01 常態分配的圖形與性質；02 68–95–99.7 規則 |
| 複數與多項式方程式 | 5 | 01 複數的意義與四則運算；02 實係數二次方程式的判別式 |
| 複數的幾何意涵 | 5 | 01 複數平面與複數的絕對值；02 複數的極式 |
| 拋物線 | 4 | 01 拋物線的定義與各部位；02 拋物線的標準式 |
| 橢圓 | 4 | 01 橢圓的定義與各部位；02 橢圓的標準式 |
| 雙曲線 | 4 | 01 雙曲線的定義與各部位；02 雙曲線的標準式 |
| 線性規劃 | 3 | 01 二元一次不等式的圖形；02 可行解區域與目標函數的最值 |

〔知識點審定單 2026-09-26〕上表是 v2.1 的狀態，第二輪有變動的列：多項式函數的圖形 5 條（sort 1／2 不變）；集合與計數原理 7 條（sort 1／2 不變；新增 06 命題與且、或、非為 sort 3、07 充分條件與必要條件為 sort 4，原 03–05 的 sort 順延為 5–7）；空間概念與座標系 6 條（sort 1／2 不變，新增 06 空間向量的坐標運算為 sort 6）；**空間向量內積 4 條，sort 1／2 改為 02 空間向量的內積與夾角；03 方向角與方向餘弦**（01 已搬走，code 空號不再使用）；函數的極限 7 條（07 勘根定理）；二項分布與幾何分布 4 條（04 已刪，05 幾何分布 sort 4）。〔知識點審定單 2026-09-26 補答〕**複數的幾何意涵 3 條，sort 1／2 改為 01 複數平面與絕對值；06 複數加減的幾何意義**（03 複數乘除的幾何意義為 sort 3；02、04、05 已刪，code 空號不再使用）。

整合時若物理、化學想引用更精確的先備，下列 code 通常比 01／02 更貼切（已換成新代碼）：

- `MATH.向量內積.01` 內積的意義（功）、`MATH.外積.02` 外積的幾何意義（力矩、勞侖茲力）、`MATH.向量的加減與係數積.03` 係數積與向量平行
- `MATH.指數與對數.04` 常用對數的定義、`MATH.指數與對數.05` 常用對數的運算性質（pH 值、分貝）；`MATH.指數函數與對數函數.01` 指數函數的圖形與性質、`MATH.指數函數與對數函數.07` 指數與對數的應用（半衰期、一級反應）
- `MATH.直角三角形的邊角關係.01` 直角三角形的三角比（力與速度的分解、折射）、`MATH.廣義角與極坐標.02` 廣義角的三角函數、`MATH.三角函數的圖形.02` 三角函數的圖形與週期（簡諧運動、交流電、波動）
- `MATH.微分導函數.01` 平均變化率與導數的定義（瞬時速度）、`MATH.定積分與面積.01` 黎曼和與定積分的意義（v-t 圖面積、功）
- `MATH.二維數據分析.04` 最小平方法與迴歸直線（實驗數據作圖）

## 4. 跨科先備引用清單

**數學 → 其他科：無。** 320 條先備全部指向數學科自己的知識點。

**其他科 → 數學**（本分支的 `物理.json`、`化學.json` 現況；兩檔由 CH-D 改寫，本分支不動）：

| 引用端 | 引用的數學 code | 依對照檔 |
|---|---|---|
| `PHYS.物體的運動（速度與加速度）.04` | `MATH.直線方程式.01` | 不變 |
| `PHYS.平面運動.01` | `MATH.向量的加減與係數積.01` | 不變 |
| `PHYS.平面運動.02` | `MATH.三角函數的定義.01` | **改為 `MATH.直角三角形的邊角關係.01`**（同一條內容） |
| `PHYS.功與動能.01` | `MATH.向量內積.01` | 不變 |
| `PHYS.幾何光學（反射折射）.04` | `MATH.三角函數的定義.01` | **改為 `MATH.直角三角形的邊角關係.01`**（同一條內容） |
| `PHYS.核物理與基本粒子.04` | `MATH.指數與對數.01` | 不變（同一條內容：指數律）。若要更貼切，可改指 `MATH.指數函數與對數函數.07`（含半衰期），但那是第三冊的內容 |

化學種子檔目前沒有指向數學的先備。`docs/kc-review-化學.md` 建議但暫不寫入的兩條數學先備，照本檔的新代碼應為：pH 值 → `MATH.指數與對數.04` 常用對數的定義；一級反應與半生期 → `MATH.指數函數與對數函數.01` 指數函數的圖形與性質（或 `.07` 指數與對數的應用）。

CH-D 分支（`stage5/ch-d` 的 `ae2c6d3`）的物理種子檔引用的數學 code（`直線方程式.01`、`一維數據分析.04`、`向量的加減與係數積.01`、`直角三角形的邊角關係.01`、`向量內積.01`、`指數與對數.01`）都不受 v2.1 的改號影響，化學種子檔沒有數學先備；三科一起驗證為 0 error（第 9.6 節）。

## 5. 「向量內積」一章的處理

本次重整完全沒有動這一章的 code、sort 與內容。

| code | 名稱 | status | 口語版來源 |
|---|---|---|---|
| `MATH.向量內積.01` | 內積的意義 | draft | 重寫（v1） |
| `MATH.向量內積.02` | 內積的坐標算法 | approved | 契約第 3.5 條逐字 |
| `MATH.向量內積.03` | 夾角與垂直 | draft | 重寫（v1） |
| `MATH.向量內積.04` | 長度平方與展開 | approved | 契約第 3.5 條逐字 |
| `MATH.向量內積.05` | 正射影 | approved | 契約第 3.5 條逐字 |
| `MATH.向量內積.06` | 柯西不等式 | approved | 契約第 3.5 條逐字 |

- 4 條 approved 與重整前的種子檔逐欄比對（整個物件序列化後相同，含 code、sort、prereqs），一字不差。它們的先備本來就只指向「向量的加減與係數積」與本章，不受重編影響。
- `.01`（draft）唯一的變動：先備 `MATH.三角函數的定義.04` 依對照檔改為 `MATH.廣義角與極坐標.02`，指向的是同一條「廣義角的三角函數」。
- 重寫的兩條沒有使用比喻，理由見 v1（內積的意義用「長度乘長度乘夾角的 cos」與投影長的幾何解讀；夾角與垂直用「長度恆正，內積的正負就是 cosθ 的正負」）。

## 6. 為什麼 `curriculum_code` 全部是 null

契約規定只有在有把握時才填 108 課綱學習內容代碼，而且不得編造。我記得代碼的結構（如 N-10-1、F-10-2、G-11A-1），但每一條的編號沒有辦法對照官方文件逐條核對，所以一律填 `null`，新寫的 43 條也一樣。之後要補，建議由 Owner 對照《十二年國教課程綱要 數學領域》學習內容表，用 `PATCH /api/kc/:id` 或知識點頁逐條補上。

## 7. 審定建議

1. 先看第 1.2 節第 1 條（指數與對數的拆法）：它決定第一冊與第三冊的分界，拆錯會讓兩冊的題目標到彼此的知識點；若要改方向，要與 CH-B 的 eval 改標一起改。
2. 第一次 `kc:load` 前確認 DB 沒有載入過數學知識點（第 2.2 節第 2 點）；有的話，先依對照檔處理題目上的標註。
3. 新章（複數的幾何意涵、拋物線、橢圓、雙曲線、線性規劃）整章新寫，建議一章一章對照課本目錄看範圍，多的刪、少的補。
4. 第 1.1 節的 10 條依 Ben 實際使用的課本版本（學生檔案的 `textbook_version`）確認慣例。
5. 「向量內積」的 `.01`、`.03` 可以當作口語版的樣本先審：這兩條如果方向對，其餘草稿寫法相同。

## 8. 審查修正紀錄（2026-09-24，v1.1）

本節保留 v1.1 當時的**舊代碼**；對應的新代碼查 `config/kc/code-maps/code-map-數學-2026-09.json`。

審查者在 v1.0 找到下列問題，已逐條查證屬實並處理；新增與修正的條目全部是 `draft`。

**補上缺漏的課綱內容**（新增 3 條，208 → 211）：

| code | 名稱 | 先備 |
|---|---|---|
| `MATH.矩陣的加減與乘法.07` | 二階方陣與平面上的線性變換 | `矩陣的加減與乘法.02`、`面積與行列式.03` |
| `MATH.矩陣的加減與乘法.08` | 旋轉、鏡射、伸縮與推移矩陣 | `矩陣的加減與乘法.07`、`正弦與餘弦定理.05` |
| `MATH.平面方程式.06` | 點對平面的投影點與對稱點 | `平面方程式.02`、`空間直線方程式.01` |

原本沒有這三條，classify 歸到這兩章的線性變換題、投影點題，tagKc 找不到合適的知識點可標。

**修正既有條目**：

| code | 欄位 | 修正 |
|---|---|---|
| `MATH.三次函數.04` 多項式不等式 | 口語版 | 原本先說「每經過一個根就變一次號」再改口；改為一開始就講清楚「奇數次的根變號、偶數次的根不變號」。反例：x(x−1)²>0 經過 1 時不變號。 |
| `MATH.排列.06` 環狀排列 | 說明 | 「可翻面者在 n≥3 時再除以 2」的條件應看圈上物件數 k，不是 n；改為「k≥3 時再除以 2，即 P(n,k)/(2k)」。反例：5 色珠取 2 顆串成環是 C(5,2)=10，不能再除以 2。 |
| `MATH.排列.01`、`MATH.組合.01` | 口語版 | 原本各 5 句，超過第 3.5 條第 6 點的 2–4 句（v1.0 第 0 節「皆 2–4 句」的說法有誤）；刪去自問句後分別為 4 句、3 句。 |
| `MATH.向量內積.01` 內積的意義 | 口語版 | 補上使用時機；「有號投影長」不是課本用語，改說「投影長，鈍角時算負的」。 |
| `MATH.一維數據分析.03` 百分位數與盒狀圖 | 口語版 | 原本只說「整數和不是整數時取法不同」；改為講出百分位數的意思與取法，與說明一致。 |
| `MATH.克拉瑪公式.03` 高斯消去法 | 口語版 | 最後一句的常見錯誤改為「列運算只對係數做，漏了常數行」。 |
| `MATH.隨機變數.04` 二項分布 | 先備 | `組合.05`（二項式定理）改為 `組合.01`（組合的意義與公式）：寫出 P(X=k) 直接用到的是 C(n,k)。 |
| `MATH.古典機率.01` 樣本空間與事件 | 先備 | 移除 `排列.02`（排容原理）：排容是 `古典機率.03` 求 P(A∪B) 才用到，該條已列為先備。 |

## 9. 章節重整紀錄（2026-09-25，CH-C）

### 9.1 做法

- 章節表以 `config/chapterPlan.js` 的 `PLAN_VOLUMES['數學']` 為準，種子檔的章順序與冊內順序照它排。
- 舊知識點依**內容**搬到新章（不是只照 `MIGRATION` 的預設去處），章內依教學順序排（`sort` 從 1 連號）。
- code：搬到另一章的條目依新章名與新序號重編（序號等於 `sort`）；留在同名章的條目沿用舊 code，空出的序號不再使用（v2.1 起；只影響指數與對數、排列兩章，見第 2.2 節第 1 點）。
- 舊檔 211 條全部保留，沒有刪除，對照檔沒有 `null`。
- 搬移的條目除第 9.3 節列出的欄位外，名稱、說明、口語版、status、curriculum_code 一字不動；先備依對照檔改寫，另有第 9.4 節的 8 處調整（9 個 code）。
- 種子檔 `version` 1 → 2，`generated_at` 改為 2026-09-25，`source_note` 註明重整與對照檔。

### 9.2 搬移對照（新章 ← 舊 code；「新」＝本次新寫）

同名而且內容、code 都不變的章不列（實數、絕對值、直線方程式 01–05、圓方程式、組合 01–04、古典機率 01–03、期望值、一維數據分析、二維數據分析、正弦與餘弦定理 01–04、三角測量、向量的加減與係數積、向量內積、面積與行列式 01–05、空間概念與座標系、空間向量內積、外積、平面方程式、空間直線方程式、數列的極限、函數的極限、微分導函數、函數圖形與極值、定積分與面積、隨機變數 01–03、常態分配）。

| 新章 | 條目（依 sort；括號內是舊 code，省略 `MATH.`） |
|---|---|
| 指數與對數 | 01 指數律與有理指數（指數與對數.01）；04 常用對數的定義（指數與對數.04，改寫）；05 常用對數的運算性質（指數與對數.05，改寫）；06 常用對數與位數估計（指數與對數.06）。code 全部沿用，sort 為 1–4 |
| 多項式的運算與應用 | 01–08（多項式除法.01–.08，一對一改名） |
| 多項式函數的圖形 | 01 一次函數與常數函數的圖形（新）；02 二次函數的圖形與最值（三次函數.01）；03 三次函數的對稱中心（三次函數.02）；04 三次函數的圖形類型（三次函數.03）；05 圖形與 x 軸的交點及重根（新）；06 勘根定理（三次函數.05） |
| 多項式不等式 | 01 一元二次不等式（新）；02 二次式恆正或恆負的條件（新）；03 高次多項式不等式（三次函數.04，改名）；04 由不等式的解反求係數（新） |
| 數列與遞迴關係 | 01 數列的一般項與遞迴式（數列與級數.01，改名）；02 等差數列（.02）；03 等比數列（.03）；04 由遞迴關係求一般項（新）；05 數學歸納法（數列與級數.08） |
| 級數 | 01 等差級數（數列與級數.04）；02 等比級數（.05）；03 Σ 符號與求和公式（.06）；04 分項對消求和（.07） |
| 集合與計數原理 | 01 集合的表示法與子集（新）；02 集合的運算與笛摩根定律（新）；03 加法原理與乘法原理（排列.01）；04 樹狀圖與有系統的列舉（新）；05 排容原理（排列.02） |
| 排列 | 03 直線排列（排列.03）；04 相鄰與不相鄰（.04）；05 不盡相異物排列（.05）；06 環狀排列（.06）；07 重複排列（.07）。code 全部沿用，sort 為 1–5 |
| 二項式定理 | 01 二項展開式的一般項（組合.05，改名）；02 二項式係數的性質（組合.06）；03 二項式定理的應用：餘數與近似值（新） |
| 直角三角形的邊角關係 | 01 直角三角形的三角比（三角函數的定義.01）；02 三角函數的基本關係（三角函數的定義.02，口語版最後一句改寫）；03 解直角三角形（新） |
| 廣義角與極坐標 | 01 廣義角與同界角（三角函數的定義.03）；02 廣義角的三角函數（.04）；03 參考角與角度化簡（.05）；04 極坐標（.07） |
| 三角函數的圖形 | 01 弧度量與扇形（三角函數的定義.06）；02 三角函數的圖形與週期（.08）；03 簡單三角方程式（新） |
| 和角與差角公式 | 01 三角函數的和角與差角公式（正弦與餘弦定理.05，改名）；02 倍角與半角公式（.06）；03 兩直線的夾角（新） |
| 三角函數的疊合 | 01 疊合公式與最大最小值（正弦與餘弦定理.07，改名）；02 二次齊次式的降次與疊合（新）；03 疊合的圖形與方程式（新） |
| 指數函數與對數函數 | 01 指數函數的圖形與性質（指數與對數.02）；02 指數方程式與不等式（.03）；03 一般底數的對數與對數律（新）；04 換底公式（新）；05 對數函數的圖形與性質（.07）；06 對數方程式與不等式（.08）；07 指數與對數的應用（新） |
| 面積與行列式 | 01–05 不變；06 二元一次聯立的克拉瑪公式（克拉瑪公式.01）；07 二元一次聯立解的判別（克拉瑪公式.02） |
| 條件機率與貝氏定理 | 01 條件機率（古典機率.04）；02 獨立事件（.05）；03 全機率與貝氏定理（.06） |
| 一次方程組 | 01 高斯消去法（克拉瑪公式.03）；02 三元一次聯立的克拉瑪公式（.04）；03 三元一次聯立解的判別（.05）；04 三平面的相交情形（.06） |
| 矩陣的運算 | 01 矩陣的意義與加減（矩陣的加減與乘法.01）；02 矩陣乘法（.02）；03 矩陣乘法的性質（.03）；04 方陣的乘冪（新）；05 二階反方陣（.04） |
| 矩陣的應用 | 01 以反方陣解聯立方程式（矩陣的加減與乘法.05）；02 轉移矩陣（.06）；03 二階方陣與平面上的線性變換（.07）；04 旋轉、鏡射、伸縮與推移矩陣（.08） |
| 二項分布與幾何分布 | 01 伯努利試驗（新）；02 二項分布（隨機變數.04）；03 二項分布的期望值與變異數（.05）；04 二項分布中機率最大的次數（新）；05 幾何分布（隨機變數.06） |
| 複數與多項式方程式 | 01 複數的意義與四則運算（新）；02 實係數二次方程式的判別式（新）；03 代數基本定理與虛根成對（三次函數.06，改寫）；04 根與係數的關係（三次函數.07）；05 高次方程式的解法（新） |
| 複數的幾何意涵 | 01–05 全部新寫 |
| 拋物線、橢圓、雙曲線 | 各 01–04 全部新寫 |
| 線性規劃 | 01 二元一次不等式的圖形（直線方程式.06）；02、03 新寫 |

### 9.3 搬移時改動內容的條目

| 新 code（舊 code） | 欄位 | 改動與理由 |
|---|---|---|
| `指數與對數.04`（code 沿用） | 名稱、說明、口語版 | v2.1：「對數的定義」（一般底數）→「常用對數的定義」：第一冊只談以 10 為底，一般底數的定義移到第三冊新寫的 `指數函數與對數函數.03`。說明改寫成 log x=c ⇔ 10ᶜ=x、log 10ⁿ=n；口語版改用 log 1000 當例子。 |
| `指數與對數.05`（code 沿用） | 名稱、說明、口語版 | v2.0：「對數律與換底公式」→「對數律」，換底公式拆到第三冊新寫（現為 `指數函數與對數函數.04`）；口語版刪掉換底的一句，補一個以 log 2、log 3 算 log 12 的例子。v2.1：再改名「常用對數的運算性質」，說明改成只談常用對數（p 為有理數，並補「可由 log 2、log 3 求出其他數的常用對數」）；口語版的例子本來就是常用對數，不動。 |
| `數列與遞迴關係.01`（`數列與級數.01`） | 名稱 | v2.1：「數列與遞迴關係」→「數列的一般項與遞迴式」：名稱原本與新章名相同，AI 標註時容易被當成萬用標籤。內容不變。 |
| `和角與差角公式.01`（`正弦與餘弦定理.05`） | 名稱 | v2.1：「和角與差角公式」→「三角函數的和角與差角公式」：同上。內容不變。 |
| `多項式不等式.03`（`三次函數.04`） | 名稱 | 「多項式不等式」→「高次多項式不等式」：名稱原本與新章名相同，且本章另有一元二次不等式。內容不變。 |
| `複數與多項式方程式.03`（`三次函數.06`） | 名稱、說明、口語版 | 「複數與代數基本定理」→「代數基本定理與虛根成對」：複數的四則運算拆成新的 `.01`，本條只談代數基本定理與虛根成對；口語版補上「給一個虛根就寫出共軛，組成實係數二次因式」的用法。 |
| `二項式定理.01`（`組合.05`） | 名稱 | 「二項式定理」→「二項展開式的一般項」：名稱原本與新章名相同。內容不變。 |
| `三角函數的疊合.01`（`正弦與餘弦定理.07`） | 名稱 | 「三角函數的疊合」→「疊合公式與最大最小值」：同上。內容不變。 |
| `直角三角形的邊角關係.02`（`三角函數的定義.02`） | 口語版最後一句 | 原本「開根號時沒有依角度所在的象限判斷正負號」，但新章只談銳角、還沒教象限；改為「把 sin²θ 看成 sin(θ²)」的常見錯誤。 |

### 9.4 先備的調整（對照檔改寫以外）

| code | 改成 | 理由 |
|---|---|---|
| `三角測量.01` 仰角與俯角 | `直角三角形的邊角關係.03`（原為三角比 `.01`） | 新章有「解直角三角形」，它才是仰角俯角題的直接先備。 |
| `多項式不等式.03` 高次多項式不等式 | `多項式不等式.01`、`多項式的運算與應用.04`（原為二次函數的圖形、因式定理） | 同章先有一元二次不等式。 |
| `集合與計數原理.05` 排容原理 | 加上 `集合與計數原理.02` | 排容用到交集與聯集。 |
| `一次方程組.01` 高斯消去法 | 空（原為矩陣的意義與加減） | 見第 2.2 節第 3 點。 |
| `二項分布與幾何分布.02`、`.05` | 以 `二項分布與幾何分布.01` 伯努利試驗取代 `隨機變數.01` | 兩種分布都建立在伯努利試驗上；獨立事件改指 `條件機率與貝氏定理.02`（對照後的同一條）。 |
| `複數與多項式方程式.03` 代數基本定理與虛根成對 | 加上 `複數與多項式方程式.01` | 複數的四則運算已拆成獨立的 `.01`，虛根成對要先會複數與共軛。（v2.0 漏記，v2.1 補上） |
| `指數函數與對數函數.05` 對數函數的圖形與性質 | `指數函數與對數函數.01`、`.03`（照表是 `.01`、`指數與對數.04`） | v2.1：`指數與對數.04` 改成只談常用對數，一般底數的對數定義在同章的 `.03`。 |
| `指數函數與對數函數.06` 對數方程式與不等式 | `指數函數與對數函數.03`、`.05`（照表是 `指數與對數.05`、`指數函數與對數函數.05`） | v2.1：解一般底數的對數方程式要用一般底數的對數律（`.03`），不是只談常用對數的 `指數與對數.05`。 |

### 9.5 對照檔 `config/kc/code-maps/code-map-數學-2026-09.json`

- **位置**：`config/kc/code-maps/`，與 CH-D 的物理、化學對照檔同一處。契約第 3.3 條第 4 點寫的是 `config/kc/`，但 `scripts/validate_kc_seed.js`（不帶參數）、`scripts/load_kc.js`（不帶 `--file`，即 `npm run kc:load`）與 `test/unit/kcSeed.test.js`「repo 內的 config/kc/*.json 全部通過」都用 `readdirSync(config/kc).filter(.json)` 讀**頂層所有** `*.json` 當種子檔，對照檔放在頂層會得到 `[?] subject 必須是 數學／物理／化學`：unit 紅燈、`kc:load` 整批拒絕。`readdirSync` 不遞迴，放子目錄就不會被讀到。這三支程式與測試沒有改。請主控在 `docs/chapter-restructure.md` 第 8 條補一筆 CR（code-map 改放 `config/kc/code-maps/`）。
- **格式**（v2.1 起，與 CH-D 相同）：扁平的 JSON 物件，鍵是舊 code，值是新 code（`null`＝刪除；數學沒有 `null`）。共 211 筆，涵蓋舊檔每一個 code，沒有兩個舊 code 指向同一個新 code。code 不變的 148 筆也列出（值等於鍵），使用端不必另外判斷「不在表內」的情形。
- v2.0 版的外層欄位移到這裡記錄：科目 數學；產出日期 2026-09-25；說明「章節重整（docs/chapter-restructure.md 第 3.3 條）的知識點代碼對照：舊 code → 新 code（null＝刪除）。先備引用與既有標註依此改寫。」；舊章 34 章（`config/chapters.js` 重整前的數學白名單）→ 新章 52 章（`config/chapterPlan.js` 的 `PLAN_VOLUMES['數學']`）。

### 9.6 驗證（2026-09-25 v2.1 修正後實跑）

- 契約指定的指令（`validateSeeds`，章節表 `{...PLAN_CHAPTERS, 化學: CHEMISTRY_CHAPTERS}`）：
  - 數學單檔：0 error、0 warning；數學 254 個、52 章、已審定 4 個。
  - 數學＋化學：0 error、0 warning。
  - 數學（本分支）＋ `stage5/ch-d` 的 `ae2c6d3` 的 `物理.json`（196 個／34 章）與 `化學.json`（236 個／44 章），從 CH-D worktree 唯讀取用：**0 error、0 warning**。
  - 數學＋本分支的舊 `物理.json`＋化學：17 個 error 全部屬於物理（15 個舊章節表、2 個指向 `MATH.三角函數的定義.01`），數學 0 個；整合 CH-D 後消失。
- 用 `kcSeed.test.js` 的讀法（`config/kc/` 頂層所有 `*.json`）配新章節表：頂層只剩 `化學／數學／物理.json` 三個檔，error 只有上一點的 17 個物理 error，**沒有對照檔的 error**。
- 暫存腳本檢查第 3.5 條可機械檢查的部分：口語版 2–4 句、40–300 字（62–231 字，平均 144 字）、無 `$` 與反斜線；說明 ≤200 字、名稱 ≤30 字、先備 ≤3 個；沒有與章名相同的名稱；章內 `sort` 從 1 連號；4 條 approved 與重整前逐欄相同。
- 對照檔：211 筆，涵蓋舊檔每一個 code，無多餘鍵，值都存在於新檔，沒有重複目標、沒有 `null`；148 筆恆等、63 筆改 code；新舊檔之間沒有同 code 異義；舊 code 消失 63 個。
- 先備與「舊先備照對照檔改寫」的差異共 9 個 code，全部列在第 9.4 節。
- 產生與檢查的腳本留在 scratchpad、不進版控（同 S5-37），之後以 JSON 本身為唯一來源。
- `npm test`：2287 個測試中 2286 個通過，**1 個失敗**：`kcSeed.test.js` 的「repo 內的 config/kc/*.json 全部通過」。它用預設章節表（`config/chapters.js`，本分支仍是舊的 34 章）驗證，共 112 個 error：104 個「數學新章不在白名單」、6 個「舊章沒有知識點」（這兩類在 CH-A 把白名單換成新章後消失）、2 個物理指向 `MATH.三角函數的定義.01`（CH-D 改寫後消失）。v2.0 另有 1 個「對照檔被當成種子檔」的 error，搬到 `code-maps/` 後已消失。基準（重整前的 `stage5/chapters-base`）是 2287 個全部通過。本分支沒有改任何測試。
- `npm run check:html`：通過。
- 沒有跑 migrate、integration、e2e 與 eval：本任務不需要資料庫，也沒有動程式與 cassette。讀程式碼判斷：`test/integration/stage5CrossWs.pg.test.js` 第 1 條以 `kcService.loadSeeds` 載入真的三科種子檔，沒有傳 `chapters`，會用 `config/chapters.js` 的舊白名單驗證，所以在本分支單獨跑時會失敗；它只讀 `數學／物理／化學.json` 三個檔名，不受對照檔影響。這一點我沒有實跑確認，CH-A 的白名單併入後應恢復。

### 9.7 審查修正紀錄（2026-09-25，v2.1）

審查者對 v2.0 提出的問題逐條查證，屬實的處理如下；新增與修正的條目都是 `draft`。

| # | 問題 | 處理 |
|---|---|---|
| 1 | 指數與對數的拆法前後矛盾：換底依「第一冊只教常用對數」搬到第三冊，但第一冊的定義與對數律仍是一般底數，第三冊沒有一般底數的定義與對數律可標（`kcTagService` 只在同章候選裡挑），而 CH-B 的改標把 log₂、log₃ 題放在第三冊 | 依原前提做完整：`指數與對數.04`、`.05` 改成常用對數版；第三冊新寫 `指數函數與對數函數.03` 一般底數的對數與對數律，其後順延（換底 `.04`、對數函數 `.05`、對數方程式 `.06`、應用 `.07`），先備同步改寫；對照檔 `指數與對數.07` → `指數函數與對數函數.05`、`.08` → `.06`。253 → 254 條。 |
| 2 | 對照檔在 `config/kc/` 頂層，會被 `kcSeed.test`、`validate_kc_seed`、`kc:load` 當成種子檔 | 移到 `config/kc/code-maps/`，格式改成與 CH-D 相同的扁平物件（第 9.5 節）。 |
| 3 | 8 個 code 在新舊檔代表不同知識點，與 CH-D「空號不再使用」不一致 | 排列、指數與對數兩章沿用舊 code（第 2.2 節第 1 點）；對照檔這 8 列改成恆等，指向它們的先備同步改寫（例如 `組合.01` 的先備 `排列.01` → `排列.03`，是同一條直線排列）。 |
| 4 | `數列與遞迴關係.01`、`和角與差角公式.01` 的名稱與新章名相同，與 v2.0 改名 3 條的標準不一致 | 改名「數列的一般項與遞迴式」「三角函數的和角與差角公式」，說明與口語版不動（第 9.3 節）。 |
| 5 | `和角與差角公式.03` 沒有排除 m₁=m₂（平行時沒有「所夾銳角」） | 說明改為「若 m₁≠m₂ 且 m₁m₂≠−1……；m₁=m₂ 時兩直線平行或重合，m₁m₂=−1 時兩直線垂直」。 |
| 6 | `橢圓.03`「A、C 同號」包含 A=C（圓） | 說明改為「A、C 同號且 A≠C」，並註明 A=C 屬於圓方程式的情形。 |
| 7 | `二項分布與幾何分布.04` 口語版「第 k 項除以第 k 減 1 項」有歧義（P(X=k) 是分布的第 k+1 項） | 改說「X 等於 k 的機率除以 X 等於 k 減 1 的機率」「這個比值不小於 1」，說明欄本來就正確、不動。 |
| 8 | 第 9.4 節漏記 `複數與多項式方程式.03` 的先備調整 | 補上；連同第 1 點新增的 2 處，第 9.4 節共 8 處（9 個 code）。 |

不在本分支處理、已轉主控：`config/chapterMigrationRules.js`（CH-A）的舊題遷移關鍵字與知識點搬移方向不一致——舊「指數與對數」的換底、指數方程式、對數方程式題，舊「三次函數」的根與係數題，舊「三角函數的定義」的參考角題，會預設留在知識點不在的章，AI 標註只在同章候選裡挑，要靠老師在 CSV 改章。

## 10. 知識點審定單第二輪落實紀錄（2026-09-26，`dec/r2-math-kc`）

〔知識點審定單 2026-09-26〕來源：Owner 在「知識點審定單」第二輪（2026-09-26 整理）對數學 M1–M21 的選擇與備註，以及第一輪 A9 的備註。前面各節的原文都保留，Owner 的決定加註在對應的項目後面；本節是總表與做法。10.1–10.4 是 `dec/r2-math-kc` 當時的紀錄；Owner 後來補答 M15、M17 並要求改寫勘根定理，落實在 `dec/r2-math-kc2`，見 **10.5**（數字以 10.5 為準）。〔重練與收尾決策單 2026-09-26〕Owner 在「重練與收尾決策單」答覆了 10.3 第 2、4 點與 10.5.3 第 2 點（K8、K9、K10），登錄與落實在 `dec/r2-math-kc3`，見 **10.6**（先備數以 10.6 為準）。

### 10.1 總表

| 代號 | 題目 | Owner 的決定 | 本分支的處理 |
|---|---|---|---|
| M1 | 「期望值放在古典機率」是指哪一種 | 選 1：「古典機率」「期望值」兩章維持分開 | 不改（第 2.1 節第 8 點） |
| M2 | 高一期望值有沒有教加法性質 | 選 1：(甲)(乙) 都有教 | 不改（第 1.1 節次要疑點） |
| M3 | 勘根定理放第一冊還是選修 | 選 1：搬到選修「函數的極限」，獨立一條 | `多項式函數的圖形.06` → `函數的極限.07`；中間值定理拿掉勘根定理這個先備、新條目以中間值定理為先備；別名「勘根定理」改指「函數的極限」（第 2.2 節第 6 點）。〔補答〕說明與口語版改寫成連續函數版本，見 10.5 |
| M4 | 邏輯、充分必要條件放哪一章 | 選 1：放「集合與計數原理」，新增 2 條 | 新增 `集合與計數原理.06`、`.07`；別名加 5 個（第 2.1 節第 4 點、第 10.2 節） |
| M5 | 空間向量的加減與坐標運算放哪一章 | 選 2：搬到「空間概念與座標系」 | `空間向量內積.01` → `空間概念與座標系.06`，3 條先備跟著改（第 2.1 節第 7 點） |
| M6 | 百分位數怎麼取 | 選 1：n×k% 法 | 不改（第 1.1 節第 1 條） |
| M7 | 轉移矩陣每一行或每一列和為 1 | 選 1：每一行和為 1 | 不改（第 1.1 節第 2 條） |
| M8 | 標準差除以 n 還是 n−1 | 選 2：只寫除以 n，不提 n−1 | 改 `一維數據分析.04` 的說明（第 1.1 節第 3 條） |
| M9 | 95% 信賴區間 2 倍還是 1.96 倍 | 選 1：寫 2 倍、括號提 1.96 | 不改（第 1.1 節第 4 條） |
| M10 | 機率最大的次數、幾何分布的 E(X)=1/p | 選 4：兩個都沒教 | 刪 `二項分布與幾何分布.04`（對照檔記 `null`）；`.05` 說明刪 E(X)=1/p，口語版一併改寫（第 1.1 節第 5 條、第 1.2 節第 4 條） |
| M11 | 數甲微分有沒有教一般連鎖律 | 選 1：兩種都寫 | 不改（第 1.1 節第 6 條） |
| M12 | 插值多項式教哪一種 | 選 1：兩種都寫、口語版用牛頓 | 不改（第 1.1 節第 7 條） |
| M13 | 算幾不等式放哪一章 | 選 1：第一冊「實數」 | 不改（第 1.1 節第 10 條） |
| M14 | Δ＝Δx＝Δy＝0 的寫法 | 選 1：加前提「都是直線時無限多解」 | 不改（第 1.1 節次要疑點） |
| M15 | 複數的幾何意涵教到哪 | 沒有選方案；備註「複數平面、乘除有教，其他都沒教」 | **暫不處理**：照字面會讓該章少於 3 條，需要再問 Owner（第 1.2 節第 2 條）。〔補答〕Owner 選「拆成 3 條、保留這章」，已處理，見 10.5 |
| M16 | 拋物線標準式用 c 還是 p | 選 1：用 c | 不改（第 1.2 節第 3 條 (a)） |
| M17 | 橢圓的面積公式與退化情形要留嗎 | 未作答 | **暫不處理**，維持現稿（第 1.2 節第 3 條 (b)(c)）。〔補答〕Owner 選「兩個都刪」，已處理，見 10.5 |
| M18 | 兩直線夾角公式放哪一章 | 選 1：第三冊「和角與差角公式」 | 不改（第 1.2 節第 5 條） |
| M19 | 三角方程式要教到一般解嗎 | 選 2：補上一般解 | 改 `三角函數的圖形.03`、`三角函數的疊合.03` 的說明與口語版（第 1.2 節第 6 條） |
| M20 | 第二冊有沒有教差集與笛摩根定律 | 選 1：都有教 | 不改（第 1.2 節第 8 條） |
| M21 | 平面方程式.06 的先備指到後一章 | 選 3：留在原章，先備改指向量係數積 | 先備 `空間直線方程式.01` → `向量的加減與係數積.03`，代碼不變（第 2.1 節第 11 點） |
| A9 | Owner 第一輪備註 | 「期望值放在古典機率、隨機變數是數甲機率統計、數學歸納法是數列與級數、推移矩陣是矩陣、複數是數甲、勘根定理是數甲微積分、反方陣也是矩陣」 | 登錄。除勘根定理（依 M3 搬移）外都與草案一致，不改（第 2.1 節第 5、8、10 點，第 2.2 節第 4–6 點） |

### 10.2 種子檔與周邊的改動

- **`config/kc/數學.json`**：254 → 255 條（刪 1、新增 2）。`version` 2 → 3，`generated_at` 改為 2026-09-26，`source_note` 補上本輪與新對照檔。4 條 `approved` 逐欄不變。除下列條目外，其他條目一字未動。
  - 搬移（名稱、說明、口語版、status 都不動）：`多項式函數的圖形.06` 勘根定理 → `函數的極限.07`（sort 7，先備 `[] → [函數的極限.06]`）；`空間向量內積.01` 空間向量的坐標運算 → `空間概念與座標系.06`（sort 6，先備不變）。
  - 新增（`draft`）：`集合與計數原理.06` 命題與且、或、非（sort 3，先備 `.02` 集合的運算與笛摩根定律）；`.07` 充分條件與必要條件（sort 4，先備 `.01` 集合的表示法與子集、`.06`）。說明寫出且或非對應交集、聯集、補集，以及 p⇒q ⇔ P⊆Q；口語版照 Owner 核可的風格（對學生說「你」、短句、最後點出常錯），各 4 句。
  - 刪除：`二項分布與幾何分布.04` 二項分布中機率最大的次數（沒有任何先備指向它）。
  - 改說明／口語版：`一維數據分析.04`（說明）、`二項分布與幾何分布.05`（說明、口語版）、`三角函數的圖形.03`、`三角函數的疊合.03`（說明、口語版）。
  - 改先備：`函數的極限.06`（拿掉 `多項式函數的圖形.06`）；`空間向量內積.02`、`外積.01`、`空間直線方程式.01`（`空間向量內積.01` → `空間概念與座標系.06`）；`平面方程式.06`（`空間直線方程式.01` → `向量的加減與係數積.03`）。
  - 只改 sort：`集合與計數原理.03`–`.05` 順延為 5–7；`空間向量內積.02`–`.05` 改為 1–4；`二項分布與幾何分布.05` 改為 4。章內 sort 仍從 1 連號；code 的序號照第 2.2 節第 1 點「空號不再使用」，不往前遞補。
- **代碼對照檔 `config/kc/code-maps/code-map-數學-2026-09-26.json`**（新檔）：格式同第 9.5 節與 CR-1，扁平物件 `{ 本輪前的 code: 本輪後的 code｜null }`。鍵是本輪前種子檔的全部 254 個 code（不變的 251 筆值等於鍵），非恆等 3 筆：`多項式函數的圖形.06` → `函數的極限.07`、`空間向量內積.01` → `空間概念與座標系.06`、`二項分布與幾何分布.04` → `null`。新增的 `集合與計數原理.06`、`.07` 本輪前不存在，不在鍵裡。舊的 `code-map-數學-2026-09.json`（v1 211 個 → v2.1）保留原樣沒有改，其中 `三次函數.05` → `多項式函數的圖形.06` 在本輪後已不是現行 code：從 v1 舊 code 換到現行 code，要兩份依序套用（先 2026-09，再 2026-09-26）。知識點尚未載入資料庫，兩份對照檔目前只供紀錄與日後改寫標註用。
- **`config/chapterAliases.js`**：「勘根定理」從「多項式函數的圖形」移到「函數的極限」（前者剩 5 個別名，仍 ≥3）；「集合與計數原理」加「邏輯」「命題」「充分條件」「必要條件」「充分必要條件」。這 5 個詞與既有別名沒有衝突：不重複、也沒有跨章的子字串關係（「必要條件」是「充分必要條件」的子字串，但兩者同章），`validateAliases()` 為空。eval 的 golden 查詢沒有用到這幾個詞，規則路徑的結果不變。
- **測試**：新增 `test/unit/kcReviewRound2Math.test.js`，釘住本輪的決定（勘根定理的位置與先備、邏輯 2 條、空間向量搬章與先備、除以 n、刪機率最大的次數與 E(X)=1/p、一般解、平面方程式.06 的先備、新對照檔、兩組別名）。既有測試沒有寫死被改掉的內容，都沒有改。

### 10.3 做了選擇或沒有做、請 Owner 知道的地方

1. **勘根定理照搬、內容沒改**：說明與口語版仍以「多項式函數」敘述，沒有放寬成「在 [a,b] 上連續的函數」；`函數的極限.06` 中間值定理的說明也仍含「特別地，f(a)f(b)<0 時至少一個實根」，口語版說「這就是勘根定理的推廣」，與新的 `.07` 有部分重疊，AI 標註時兩條都可能被選。Owner 沒有決定要不要改寫，維持原狀。〔補答〕Owner 已決定改寫，兩條不再重疊，見 10.5。
2. **邏輯 2 條的 sort**：審定單沒有指定順序。放在集合運算之後、計數原理之前（sort 3、4），因為兩條都以集合為先備；若 Ben 的課本先教邏輯再教集合，只要改 sort 與先備，代碼不必動。〔重練與收尾決策單 2026-09-26 K8〕Owner 選 1：維持在集合運算之後、計數原理之前，不改；只登錄（第 10.6 節）。
3. **M8 的口語版沒有改**：口語版原本就只講「除以 n」，沒有提 n−1，所以只改說明。「母體變異數」改稱「變異數」，因為不再和樣本變異數對照。
4. **M10 的口語版**：審定單的選項只寫「說明刪這句」，但口語版也講了「期望值是 p 分之 1，平均要擲 6 次」；Owner 選的是「沒教」，所以一併刪掉，改用「第 3 次才出現 6 的機率」當例子。Owner 若要保留口語版那句，改回即可。〔重練與收尾決策單 2026-09-26 K9〕Owner 選 1：同意口語版一起改的做法，維持現稿；只登錄（第 10.6 節）。
5. **沒有動的周邊**：
   - `config/chapterExamples.js` 的分類例句：M3、M4、M5 只動知識點與查題別名，審定單寫明不重錄；改例句會讓 classify 需要重錄（CR-7）。
   - `config/chapterPlan.js` 的 `MIGRATION`：舊「三次函數」的去處沒有「函數的極限」，舊題若是勘根題，搬章工具仍會預設提議「多項式函數的圖形」，要老師在 CSV 改。這是章節結構，不在本輪審定範圍。
   - `utils/tokenize.js` 的詞典沒有加「命題」「充分條件」等新詞：改分詞會影響檢索與 eval。
   - 「空間概念與座標系」的查題別名沒有加空間向量加減類的詞（M5 只決定知識點位置）。若要讓「空間向量的加減」這類查詢直接對到這章，可另加別名；「空間向量」本身不能收，因為它是「空間向量夾角」（→空間向量內積）的子字串。
6. **M15、M17 暫不處理**，見 10.1。〔補答〕Owner 已補答，見 10.5。

### 10.4 驗證（2026-09-26 實跑）

- `npm run kc:validate`：數學 255 個、52 章、已審定 4 個；物理 196、化學 236；三科一起 0 error、0 warning。
- 暫存腳本（不進版控）另外檢查：4 條 approved 與本輪前逐欄相同；章內 sort 從 1 連號，陣列順序依白名單與 sort；先備 322 條、每條 0–3 個、無環，**沒有任何先備指向後面的章或同章後面的條目**；口語版 62–234 字、全部 2–4 句、無 LaTeX；說明最長 179 字；對照檔涵蓋本輪前全部 254 個 code，非 `null` 的值都在新種子檔。
- `bash /home/claude/ci.sh <worktree>/exam_pro tutor_r2math_test quick`：unit 2726 個測試，2724 通過、0 失敗（另 2 個為既有的略過）；check:html 通過。
- 沒有跑 migrate、integration、e2e、eval：本輪只改種子檔、別名與文件，不需要資料庫，也沒有動提示詞與 cassette。

### 10.5 Owner 補答的落實（2026-09-26，`dec/r2-math-kc2`）

〔知識點審定單 2026-09-26 補答〕Owner 在 10.1 之後補答了 M15、M17，並對 M3 的後續（勘根定理的寫法）做了決定。本分支接在 `dec/r2-math-kc` 之後。

#### 10.5.1 總表

| 代號 | Owner 的答案 | 本分支的處理 |
|---|---|---|
| M15 | 課本只教複數平面與乘除，極式、棣美弗定理、n 次方根都沒教；選「拆成 3 條、保留這章」：複數平面與絕對值、複數加減的幾何意義、複數乘除的幾何意義 | 「複數的幾何意涵」5 → 3 條：`.01` 複數平面與絕對值（sort 1，code 沿用、改名、說明收窄）、`.06` 複數加減的幾何意義（sort 2，新增）、`.03` 複數乘除的幾何意義（sort 3，code 沿用、改名、改寫）；刪 `.02` 複數的極式、`.04` 棣美弗定理、`.05` 複數的 n 次方根（對照檔記 `null`） |
| M17 | 選「兩個都刪」 | `橢圓.04` 說明刪「∠F₁PF₂=θ 時面積為 b²·tan(θ/2)」，保留 2a＋餘弦定理的算法；`橢圓.03`、`雙曲線.04` 說明與口語版刪配方後的退化情形（只剩一點、沒有圖形、兩相交直線）；代碼、名稱、先備不變 |
| M3 後續 | 勘根定理屬數甲微積分（已是 `函數的極限.07`），改寫成連續函數版本、以中間值定理為先備；中間值定理改為一般敘述，「f(a)f(b)<0 至少一根」交給勘根定理，只留一句「勘根定理是它的特例」 | `函數的極限.07` 說明與口語版改寫；`函數的極限.06` 說明刪「特別地，f(a)f(b)<0 時……」，改成「勘根定理是它的特例」，口語版一併調整；兩條的名稱、代碼、先備不變 |

#### 10.5.2 種子檔與周邊的改動

- **`config/kc/數學.json`**：255 → 253 條（刪 3、新增 1）。`version` 仍是 3、`generated_at` 仍是 2026-09-26：同一輪審定單、同一天，`dec/r2-math-kc` 也還沒有併進任何分支（見 10.5.3 第 7 點）。4 條 `approved` 逐欄不變。除下列條目外，其他條目一字未動。
  - `複數的幾何意涵.01`（sort 1）：名稱「複數平面與複數的絕對值」→「複數平面與絕對值」（Owner 的用詞）。說明：z=a+bi 對應點 (a,b)、|z|=√(a²+b²) 是到原點的距離、|z₁−z₂| 是兩點距離，保留 z·z̄=|z|²；「共軛複數是對實軸的對稱點」移到 `.06`。口語版內容不變，只把例子改成對學生說「你就知道 z 在以 (1,0) 為圓心、半徑 2 的圓上」。先備不變（`複數與多項式方程式.01`）。
  - `複數的幾何意涵.06` 複數加減的幾何意義（新增，sort 2，`draft`）：複數看成由原點出發的向量，z₁+z₂ 是向量相加，z₁−z₂ 是由 z₂ 指向 z₁ 的向量，加上 w=c+di 是平移 (c,d)，共軛 z̄=a−bi 是對實軸的鏡射。口語版 4 句，例子「加上 2 減 i 就是往右移 2、往下移 1」，常錯是 z 減 w 的方向畫反。先備：`複數的幾何意涵.01`、`向量的加減與係數積.02` 向量的加法與減法。
  - `複數的幾何意涵.03`（sort 3）：名稱「極式的乘除與旋轉」→「複數乘除的幾何意義」。說明改成不用極式的寫法：先定義輻角（正實軸為始邊、原點到 z 的射線為終邊的角），乘上 z 是到原點的距離變 |z| 倍、繞原點轉 z 的輻角，除以 z 相反（1/|z| 倍、反方向轉），例：乘 i 是逆時針轉 90°。口語版 3 句，例子 (3+2i)·i=−2+3i（已驗算），常錯是除法轉錯方向。先備 `[.02 複數的極式, 和角與差角公式.01]` → `[.01, 廣義角與極坐標.04, 和角與差角公式.01]`：刪掉的 `.02` 以它自己的先備（`.01`、`廣義角與極坐標.04` 極坐標）代入，和角公式維持（見 10.5.3 第 2 點）。〔重練與收尾決策單 2026-09-26 K10〕Owner 決定拿掉和角公式這個先備，先備改為 `[.01, 廣義角與極坐標.04]`（第 10.6 節）。
  - 刪除：`複數的幾何意涵.02` 複數的極式、`.04` 棣美弗定理、`.05` 複數的 n 次方根。刪除前確認：指向它們的先備只有同章的 `.03`→`.02`、`.04`→`.03`、`.05`→`.04`，其他章沒有任何先備指向這三條；`.03` 已改指上一點的先備。
  - `橢圓.04`：說明刪 b²·tan(θ/2) 那一句，改成「結合 F₁F₂=2c 與餘弦定理可求 PF₁·PF₂、∠F₁PF₂ 或面積。」口語版原本就只講 2a＋餘弦定理、沒有提這個公式，未改。
  - `橢圓.03`：說明刪「但也可能只是一點或沒有圖形」，配方那句改成「（A、C 同號且 A≠C）**表示橢圓時**，可配方化為此形式」；「A=C 時屬於圓方程式的情形」不是退化情形（v2.1 審查第 6 點補的），保留。口語版最後一句原本講「右邊是 0 只剩一點、負的沒有圖形」，改成「配方前沒先把 x 平方、y 平方前面的係數提出來，或右邊還沒化成 1 就直接讀分母」。
  - `雙曲線.04`：說明刪「配方後，若右邊為 0，則圖形為兩相交直線」，改成「Ax²+Cy²+Dx+Ey+F=0（A、C 異號）**表示雙曲線時**，可配方化為此形式，以求中心、焦點與漸近線」。口語版最後一句原本講「右邊是 0 變成兩條相交直線」，改成「漸近線還寫成通過原點的直線，忘了它要通過新的中心 (h,k)」。
  - `函數的極限.07` 勘根定理：說明「若函數 f 在閉區間 [a,b] 上連續，且 f(a)f(b)<0，則方程式 f(x)=0 在開區間 (a,b) 內至少有一個實根；常用來判斷根的位置，並配合二分法估計根的近似值。」口語版 3 句：連續＋一正一負 → 至少一根、是中間值定理的特例；代整數找變號、再用二分法取中點縮小範圍；常錯是反過來用（同號中間仍可能有兩根）。先備仍是 `函數的極限.06`。
  - `函數的極限.06` 中間值定理：說明改為「若 f 在閉區間 [a,b] 上連續，則 f 在 [a,b] 上取遍介於 f(a) 與 f(b) 之間的每一個值；勘根定理是它的特例。」口語版也拿掉「一端正一端負中間一定有根，這就是勘根定理的推廣」，改成一句「一端正、一端負中間一定有根的勘根定理，就是它的特例」；不連續的反例仍用 x 分之 1，改說成「介於中間的 0 取不到」（中間值定理的一般敘述），不再說成「沒有根」。
  - 口語版都照 Owner 核可的風格（對學生說「你」、短句、最後一句點出常錯），3–4 句、無 LaTeX。
- **代碼對照檔 `config/kc/code-maps/code-map-數學-2026-09-26.json`**（同一份，補上本次）：鍵仍是 `dec/r2-math-kc` 之前種子檔的全部 254 個 code。新增 3 筆 `null`：`複數的幾何意涵.02`、`.04`、`.05`；`.01`、`.03` 沿用 code，仍是恆等。非恆等共 6 筆（搬移 2、`null` 4）。新增的 `複數的幾何意涵.06` 本輪前不存在，不在鍵裡（與 `集合與計數原理.06`、`.07` 同樣記在本文件）。對照檔是「本輪前 → 現在」，只看過 `dec/r2-math-kc` 狀態的使用端直接套用也正確：那個狀態多出來的 code 都不是鍵，本次也沒有改它們的代碼。
- **`config/chapterAliases.js`**：沒有改（見 10.5.3 第 4 點）。
- **測試** `test/unit/kcReviewRound2Math.test.js`：新增 3 個測試（M3 後續、M15、M17）；對照檔那一個測試的斷言跟著 Owner 的新決定改：`null` 由 1 筆改為逐筆列出的 4 筆，「不是任何舊 code 的去處」由 2 條改為逐條列出的 3 條，另加 `.01`、`.03` 恆等的斷言。仍是精確比對，沒有放寬。其他既有測試沒有寫死被改掉的內容，都沒有改。

#### 10.5.3 做了選擇、請 Owner 知道的地方

1. **code 怎麼配**：`.01`、`.03` 是同一個概念的範圍調整（`.01` 少了共軛的幾何意義；`.03` 從「用極式算乘除」改成「乘除的幾何意義」，講的仍是乘法＝伸縮＋旋轉），照 `指數與對數.04`、`.05` 的前例沿用 code；只有「加減的幾何意義」是新概念，給新 code `.06`（`.02`、`.04`、`.05` 是空號，不重用）。所以本章 code 的序號是 01、06、03，對應 sort 1、2、3，與 `排列`、`集合與計數原理` 一樣序號不等於 sort。`.02` 極式、`.04`、`.05` 記 `null`（刪除），沒有記成搬到 `.03`：極式這個寫法整個不教，輻角只在 `.03` 的說明裡帶一句。若 Owner 希望標在「極式」的舊題改標到 `.03`，把對照檔的 `.02` 改成 `.03` 即可。
2. **`.03` 的先備保留和角公式**：原本以和角公式為先備，是因為極式乘法要用它證明；課本不教極式後，乘法是轉角相加的理由是否仍用和角公式推，我不確定，所以維持。若課本只用例子說明，可以拿掉，改成只有 `.01`、`廣義角與極坐標.04`。`.06` 的先備（向量的加法與減法）是新條目自己需要的，不是從別處搬來。〔重練與收尾決策單 2026-09-26 K10〕Owner 選 1：拿掉和角公式，保留其他先備；已改成只有 `.01`、`廣義角與極坐標.04`（第 10.6 節）。
3. **`.01` 的 z·z̄=|z|²**：Owner 列的 `.01` 內容沒有提到它，但它講的是絕對值，也沒有說要刪，所以留在 `.01`；只有「共軛是對實軸的鏡射」照 Owner 的分法移到 `.06`。
4. **沒有動的周邊**（都是查題、分類用，不是知識點本身）：
   - 查題別名「複數的幾何意涵」仍是「棣美弗定理」「棣美弗」「極式」「高斯平面」「複數平面」：別名只負責把查詢導到章，保留後老師打「棣美弗定理」仍會導到這章（這章已沒有對應的知識點）。另外 `test/unit/chapterRestructure.test.js` 與 `test/integration/nlq.pg.test.js` 用「棣美弗定理」當新章別名的例子，拿掉要一起改測試。沒有「n 次方根」這個別名。
   - `config/chapterExamples.js` 的分類例句，「複數的幾何意涵」那句是棣美弗定理的題目（「以棣美弗定理求 (1+√3i)⁶……」）。改例句會讓 classify 要重錄（CR-7），沒有改；若 Owner 要改，建議換成「乘 i 旋轉」這類題目，並一起重錄。
   - `utils/tokenize.js` 的詞典仍有「棣美弗定理」「極式」：改分詞會影響檢索與 eval，沒有改。
5. **M17 的說明補了「表示橢圓時」「表示雙曲線時」**：只刪退化情形的話，「Ax²+Cy²+…=0（A、C 同號）可配方化為橢圓標準式」這句本身不一定成立（退化時化不成），加這幾個字讓句子仍然正確，但不提退化時是什麼圖形。雙曲線那句原本只寫退化的情形，刪掉後補成和橢圓對稱的「可配方化為此形式，以求中心、焦點與漸近線」。口語版最後一句原本都在講退化情形，換成別的常錯（10.5.2）。
6. **中間值定理的口語版也改了**：Owner 指定的是「說明」，但口語版有「一端正、一端負，中間一定有根，這就是勘根定理的推廣」，和勘根定理重疊，所以一併改成「勘根定理是它的特例」。Owner 若要保留原口語版，改回即可。
7. **種子檔 `version` 沒有再加 1**：本次和 `dec/r2-math-kc` 都是第二輪審定單、同一份對照檔，而 `dec/r2-math-kc` 還沒有併進任何分支。若先單獨合併 `dec/r2-math-kc`、之後才合併本分支，兩個狀態都會是 `version` 3，屆時可考慮把本分支改成 4。

#### 10.5.4 驗證（2026-09-26 實跑）

- `npm run kc:validate`：數學 253 個、52 章、已審定 4 個；物理 196、化學 236；三科一起 0 error、0 warning。
- 暫存腳本（不進版控）另外檢查：4 條 approved 與本次之前逐欄相同；章內 sort 從 1 連號，陣列順序依白名單與 sort；先備 321 條、每條 0–3 個、無環，沒有任何先備指向後面的章或同章後面的條目；口語版 62–234 字、全部 2–4 句、無 LaTeX；說明最長 179 字；對照檔 254 筆，鍵等於 `dec/r2-math-kc` 之前的全部 code，非 `null` 的值都在種子檔；沒有重用過去用過的 code。與 `dec/r2-math-kc` 相比，改動的條目只有 10.5.2 列的 11 個 code（改 7、增 1、刪 3）。
- `bash /home/claude/ci.sh <worktree>/exam_pro tutor_math2_test quick`：unit 2729 個測試，2727 通過、0 失敗（另 2 個為既有的略過）；check:html 通過。
- 沒有跑 migrate、integration、e2e、eval：本次只改種子檔、對照檔、一個單元測試與文件，沒有動別名、提示詞與 cassette。

### 10.6 重練與收尾決策單的登錄（2026-09-26，`dec/r2-math-kc3`）

〔重練與收尾決策單 2026-09-26〕Owner 在「重練與收尾決策單」答覆了本文件先前留給 Owner 的三點：10.3 第 2 點（K8）、10.3 第 4 點（K9）、10.5.3 第 2 點（K10）。本分支接在 `dec/r2-math-kc2` 之後；答覆也加註在上述各點與第 1.1 節第 5 條、第 2.1 節第 4 點、第 10.5.2 節 `.03` 那一項。

#### 10.6.1 總表

| 代號 | 題目 | Owner 的決定 | 本分支的處理 |
|---|---|---|---|
| K8 | 邏輯 2 條（`集合與計數原理.06` 命題與且、或、非，`.07` 充分條件與必要條件）放在章內哪裡 | 選 1：維持在集合運算之後、計數原理之前 | 不改，只登錄。sort 仍是 `.01`、`.02`、`.06`、`.07`、`.03`、`.04`、`.05` 對 1–7 |
| K9 | M10 幾何分布（`二項分布與幾何分布.05`）的口語版一起刪掉 E(X)=1/p、改用「第 3 次才出現 6」當例子，這個做法可不可以 | 選 1：同意 | 不改，只登錄 |
| K10 | `複數的幾何意涵.03` 複數乘除的幾何意義要不要保留「和角與差角公式」這個先備 | 選 1：拿掉「和角與差角公式」，其他先備保留 | 先備 `[.01, 廣義角與極坐標.04, 和角與差角公式.01]` → `[.01, 廣義角與極坐標.04]` |

#### 10.6.2 種子檔與周邊的改動

- **`config/kc/數學.json`**：只改 `複數的幾何意涵.03` 的 `prereqs`，拿掉 `MATH.和角與差角公式.01`；名稱、說明、口語版、sort、status、code 都不動，說明與口語版本來就沒有提到和角公式。知識點仍是 253 條；先備 321 → 320 條。其他 252 條與檔頭（`version`、`generated_at`、`source_note`）一字未動，4 條 `approved` 逐欄不變。`和角與差角公式.01` 仍是 `和角與差角公式.02`、`.03`、`三角函數的疊合.01`、`矩陣的應用.04` 的先備，不會變成沒有後續的條目。
- **代碼對照檔**：沒有改。K10 只動先備，沒有 code 搬移或刪除。
- **`config/chapterAliases.js`、`config/chapterExamples.js`、`utils/tokenize.js`**：沒有改。
- **測試** `test/unit/kcReviewRound2Math.test.js`：新增一組「〔重練與收尾決策單 2026-09-26〕數學種子檔」共 3 個測試：K8 逐條釘住「集合與計數原理」的 code 與 sort 順序；K9 釘住幾何分布口語版不講期望值、用「第 3 次才出現」當例子（原本的 M10 測試只檢查說明）；K10 精確比對 `.03` 的先備。既有的斷言都沒有改。

#### 10.6.3 請 Owner 知道的地方

1. **K8、K9 沒有改任何資料**：兩者都是確認現狀，只在文件登錄，並加測試釘住，之後若要改順序或口語版，要連同測試一起改。
2. **種子檔 `version` 沒有再加 1**：理由同 10.5.3 第 7 點；本次只拿掉一個先備，`dec/r2-math-kc`、`dec/r2-math-kc2` 也都還沒有併進任何分支。若分開合併，屆時可考慮依序改成 4、5。

#### 10.6.4 驗證（2026-09-26 實跑）

- `npm run kc:validate`：數學 253 個、52 章、已審定 4 個；物理 196、化學 236；種子檔全部通過。
- 暫存腳本（不進版控）另外檢查：與 `dec/r2-math-kc2` 相比，只有 `複數的幾何意涵.03` 一條不同，差異只在 `prereqs`；檔頭相同；4 條 approved 逐欄相同；先備 320 條、無環，沒有任何先備指向陣列中後面的條目。
- `bash /home/claude/ci.sh <worktree>/exam_pro tutor_math3_test quick`：unit 2732 個測試，2730 通過、0 失敗（另 2 個為既有的略過）；check:html 通過。
- 沒有跑 migrate、integration、e2e、eval：本次只改種子檔的一個先備、一個單元測試與文件。
