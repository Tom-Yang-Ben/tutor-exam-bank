{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "lint.v1 — 公式重寫（第三層，只有硬閘門擋下來才會呼叫）",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "question_text",
    "answer_text",
    "notes"
  ],
  "properties": {
    "question_text": {
      "type": "string",
      "minLength": 1,
      "description": "修好公式後的題幹；除了公式寫法之外，一個字都不准改"
    },
    "answer_text": {
      "type": "string",
      "description": "修好公式後的答案；原本是空字串就回空字串"
    },
    "notes": {
      "type": "string",
      "maxLength": 200,
      "description": "改了什麼（≤200 字），會進 job_events.detail"
    }
  }
}
